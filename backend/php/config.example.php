<?php
/**
 * Copia este archivo como config.php y pon tu API key real.
 *
 * IMPORTANTE:
 * - config.php NUNCA se sube a GitHub (está en .gitignore).
 * - Si tu hosting lo permite, mejor aún: define GEMINI_API_KEY como
 *   variable de entorno en el panel y borra config.php.
 * - Si alguna vez has pegado tu key en un chat, email o archivo compartido,
 *   regenérala en https://aistudio.google.com/apikey antes de usarla aquí.
 */
return [
    'GEMINI_API_KEY' => 'PEGA_AQUI_TU_API_KEY',
];
